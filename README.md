# AGENTS.md at HOME

このリポジトリはホームディレクトリ配下の `.agents` を記載します。

## 使用方法

Antigravity CLI の Global Rules が本ディレクトリを参照するように、`~/.gemini/GEMINI.md` にその旨の文章を追記してください。

**追記例:**
```markdown
AIエージェントはGlobal Rule を読み込む際に、必ず `~/.agents/` ディレクトリ配下にあるルール（`~/.agents/AGENTS.md` など）を参照し、遵守してください。
```
